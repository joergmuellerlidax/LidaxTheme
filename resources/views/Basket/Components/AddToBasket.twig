{{ component( "Ceres::Item.Components.QuantityInput" ) }}

<script type="x/template" id="vue-add-to-basket">

    <div v-if="showQuantity">
        <span>
            <div class="col-xs-12 col-sm-6 col-md-12 col-lg-6 m-y-1">
                <quantity-input :value="1" @quantity-change="updateQuantity" template="#vue-quantity-input"></quantity-input>
            </div>
            <div class="col-xs-12 col-sm-6 col-md-12 col-lg-6 m-y-1">
                <button href="#" class="btn btn-block btn-primary" @click="addToBasket()">
                    {{ trans("Ceres::Template.addToBasket") }}
                    <i class="fa fa-shopping-cart" aria-hidden="true"></i>
                </button>
            </div>
        </span>
    </div>

    <div v-else>
        <div class="AddToBasket-inner">
            <div class="eek-sign" data-toggle="modal" data-target="#availability-modal">
                <span>A++</span>
            </div>
            <button type="button" class="btn btn-block btn-buy" @click="addToBasket()">
                Jetzt ansehen
            </button>
            <div class="availability-preview" data-toggle="tooltip"
data-placement="top" title="Lieferbar in 1-2 Werktagen">
                <i class="fa fa-truck" aria-hidden="true"></i>
            </div>
        </div>
        
        {# maybe use the button for wishlist #}
        {#<button type="button" class="btn btn-primary" @click="directToItem()">#}
            {#<i class="fa fa-arrow-right" aria-hidden="true"></i>#}
        {#</button>#}
        
    </div>

</script>